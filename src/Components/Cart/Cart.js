import React from 'react';
import Qusans from '../Quans/Qusans';

const Cart = ({ cart }) => {
    const { name } = cart
    return (
        <div>

            <li>{name}</li>









        </div>
    );
};

export default Cart;